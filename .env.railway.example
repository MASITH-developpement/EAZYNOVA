# ==========================================
# Variables d'Environnement Railway - EXEMPLE
# ==========================================
# Copiez ces variables dans l'interface Railway
# Ne commitez JAMAIS ce fichier avec des vraies valeurs !

# === ENVIRONNEMENT ===
ENVIRONMENT=production

# === ODOO ADMIN ===
# IMPORTANT: Changez ce mot de passe !
# Générez un mot de passe sécurisé: openssl rand -base64 32
ODOO_ADMIN_PASSWORD=ChangeMeToSecurePassword123!

# === POSTGRESQL ===
# Ces variables sont automatiquement fournies par Railway
# Quand vous ajoutez un service PostgreSQL
# Vous pouvez les référencer dans votre service Odoo

# PGHOST=postgres.railway.internal
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=<généré par Railway>
# PGDATABASE=railway

# Ou utilisez DATABASE_URL directement
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# === PORT HTTP ===
# Railway fournit automatiquement cette variable
# PORT=8069

# === CONFIGURATION EMAIL (OPTIONNEL) ===
# Décommentez et configurez si vous voulez envoyer des emails

# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre-email@gmail.com
# SMTP_PASSWORD=votre-mot-de-passe-application
# SMTP_SSL=False
# EMAIL_FROM=noreply@votredomaine.com

# === DOMAINE PUBLIC (OPTIONNEL) ===
# Railway fournit automatiquement RAILWAY_PUBLIC_DOMAIN
# Vous pouvez aussi définir un domaine personnalisé

# PUBLIC_URL=https://eazynova.votredomaine.com

# === CONFIGURATION AVANCÉE (OPTIONNEL) ===

# Nombre de workers (production)
# WORKERS=2

# Threads cron
# MAX_CRON_THREADS=2

# Niveau de logs
# LOG_LEVEL=info

# Mode développeur (ne PAS activer en production)
# DEV_MODE=False

# === NOTES ===
# 1. Dans Railway, ajoutez ces variables via l'interface web
# 2. Les variables PostgreSQL sont automatiquement injectées
# 3. Ne stockez JAMAIS de secrets dans le code ou Git
# 4. Utilisez des mots de passe forts et uniques
# 5. Activez l'authentification à deux facteurs sur Railway
